@model IEnumerable<LoadsheddingSystem.Models.ComplaintLetter>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Username)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Reason)
        </th>
     
        <th>
            @Html.DisplayNameFor(model => model.ComplaintDate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Username)
        </td>
       
        <td>
            @Html.DisplayFor(modelItem => item.Comments)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ComplaintDate)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ComplaintLetterId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ComplaintLetterId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ComplaintLetterId })
        </td>
    </tr>
}

</table>
